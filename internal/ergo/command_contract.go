package ergo

const (
	NewTaskUsage = `usage: ergo new task "<title>" [--epic <id>] [--draft]; optional piped stdin becomes the body`
	NewEpicUsage = `usage: ergo new epic "<title>" --file <path> [--draft]; optional piped stdin becomes the epic body`
)
